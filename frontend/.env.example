# Frontend (Desktop client) environment

# Note: ChromeDriver path is not read from .env.
# The client auto-discovers a driver next to the built binary in `dist/`
# (or falls back to PATH / Selenium Manager).

# Chrome remote debugging endpoint
DEBUGGER_ADDRESS=localhost:9222

# Backend API base URL used by the client
# For packaged builds, place this .env next to the executable
API_BASE_URL=http://localhost:8000

# Seconds to wait between successful Normal iterations
SLEEP_BETWEEN_OK_RUNS=3
